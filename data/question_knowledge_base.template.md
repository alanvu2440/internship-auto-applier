# Question Knowledge Base

This file is auto-generated and logs every question the bot encounters + the answer used.
It is gitignored because it may contain personal info from your applications.

## Format
Each entry looks like:
```
### [TIMESTAMP] Company — Role
**Question:** Why do you want to work here?
**Answer (config):** [your answer]
```

## Purpose
- Review this after runs to catch any bad `generic_fallback` answers
- If you see a wrong answer, add the correct one to `config/master_config.yaml` under `common_answers`

